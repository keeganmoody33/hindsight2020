# Inboxes

| email | domain | provisioned | warmup_started | ready_at | status |
| --- | --- | --- | --- | --- | --- |
| _none yet_ | | | | | **not provisioned** |

`ready_at = warmup_started + ~30 days`. The send skill refuses any inbox with `status <> ready`.
